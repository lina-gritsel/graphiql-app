import { FC } from 'react'

import styles from './Header.module.scss'

import Menu from '../Menu'

const Header: FC = () => {
  return (
    <div>
      <Menu />
    </div>
  )
}

export default Header
